# [功能名] 测试用例

> 关联 spec: `specs/feature-name/spec.md`
> 关联接口: `design/api-contracts.md#模块名`
> 最后更新: <!-- YYYY-MM-DD -->
> 用例状态: 待评审

---

## 一、需求测试用例

> 根据 spec 中的功能需求、页面交互、质量需求整理。

### 1.1 功能需求验证

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-FR-001 | <!-- 场景描述 --> | <!-- 前置条件 --> | 1. xxx | <!-- 预期结果 --> | P0 |

### 1.2 页面交互验证

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-UI-001 | 加载态展示 | 进入页面 | 观察页面加载过程 | 显示加载动画 | P1 |
| TC-UI-002 | 空态展示 | 数据为空 | 进入页面 | 显示空态提示 | P1 |
| TC-UI-003 | 错误态展示 | 接口返回错误 | 进入页面 | 显示错误提示和重试按钮 | P1 |

### 1.3 边界场景

| 用例ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-BD-001 | <!-- 边界描述 --> | <!-- 前置条件 --> | <!-- 操作步骤 --> | <!-- 预期结果 --> | P1 |

---

## 二、接口测试用例

> 根据 api-contracts.md 中的接口定义整理。纯前端功能可省略此章节。

### 2.1 [接口名称] `POST /api/v1/xxx`

#### 正常流程

| 用例ID | 测试场景 | 请求参数 | 预期响应 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-API-001 | 正常请求 | `{ "field": "value" }` | `{ code: 0, data: {...} }` | P0 |

#### 参数校验

| 用例ID | 测试场景 | 请求参数 | 预期响应 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-API-002 | 必填字段缺失 | `{ }` | `{ code: 422 }` | P0 |

#### 错误码验证

| 用例ID | 测试场景 | 触发条件 | 预期错误码 | 预期前端处理 | 优先级 |
|--------|---------|---------|-----------|------------|--------|
| TC-API-003 | 未登录 | 不带 token | 401 | 跳转登录页 | P0 |

#### 边界值

| 用例ID | 测试场景 | 请求参数 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-API-004 | 空列表 | 无数据 | 返回空数组 | P1 |

---

## 三、变更记录

| 日期 | 变更内容 | 关联变更 |
|------|---------|---------|
| <!-- YYYY-MM-DD --> | 初始创建 | <!-- changes/change-slug --> |
